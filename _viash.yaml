viash_version: 0.8.2

source: src
target: target

config_mods: |
  .functionality.version := '0.1.0'
  .functionality.requirements.commands := ['ps']
  .platforms[.type == 'docker'].target_registry := 'viash-hub.com:5050'
  .platforms[.type == 'docker'].target_organization := 'data-intuitive/demo'
  .platforms[.type == 'docker'].target_image_source := 'https://viash-hub.com/data-intuitive/demo'
  .platforms[.type == 'nextflow'].directives.tag := '$id'
  .platforms[.type == 'nextflow'].auto.simplifyOutput := false
